node_modules/
dist/
dist-node/
build/
.vite/
*.tsbuildinfo
coverage/
.env
.env.*
!.env.example
apps/api/drizzle/meta/_journal.json.bak
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*
pnpm-debug.log*

# Owner-provided source photo with EXIF/GPS — never commit; only the
# scrubbed apps/web/public/turing.jpg derivative is committed.
apps/web/assets/turing.jpg

# Brainstorm visual-companion scratch
.superpowers/

# Claude Code local state: git worktrees under .claude/worktrees/, the
# user-local settings file (settings.local.json), scheduled-task lockfiles,
# etc. Nothing under .claude/ belongs in the repo. Other agent dirs
# (.cursor/, .aider*, .continue/, .codeium/) can join when they appear.
.claude/
